import React from "react";
import styled from "styled-components";

export default () => {
  return <Card />;
};

const Card = styled.div`
  background: red;
  border-radius: 6px;
  position: relative;
`;
